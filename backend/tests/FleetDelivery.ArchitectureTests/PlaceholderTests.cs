namespace FleetDelivery.ArchitectureTests;

// Placeholder: proves the project compiles with NetArchTest.Rules wired in.
// Real module-boundary rules (e.g. a module's Domain/Application may not
// reference another module's Infrastructure; cross-module calls must go
// through the other module's Application contracts) get added once modules
// exist, starting M1.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_test_passes()
    {
        Assert.True(true);
    }
}
