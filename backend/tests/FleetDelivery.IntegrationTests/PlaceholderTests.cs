namespace FleetDelivery.IntegrationTests;

// Placeholder: proves the project compiles with Testcontainers.PostgreSql
// and Testcontainers.RabbitMq wired in. Replace with real integration tests
// (spinning up Postgres/RabbitMQ containers) once the Identity module lands
// in M1.
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_test_passes()
    {
        Assert.True(true);
    }
}
