/**
 * Placeholder landing page. Real dispatcher (desktop-first) and driver
 * (mobile-first) routes land per-milestone under src/features/* — see
 * docs/ARCHITECTURE.md.
 */
export function UnderConstructionPage() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-2 px-4 text-center">
      <h1 className="text-xl font-medium">Fleet & Delivery Management System</h1>
      <p className="text-[var(--color-text-muted)]">Under construction.</p>
    </main>
  )
}
