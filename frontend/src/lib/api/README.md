# lib/api

Placeholder for the typed API client generated (or hand-written) against the
backend's OpenAPI contract, per `docs/ARCHITECTURE.md`. The contract isn't
published yet — this folder stays empty until it is.
